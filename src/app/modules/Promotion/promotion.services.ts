/**
 * Stub: Promotion services — implement when Promotion module is built.
 */
export const expireOverduePromotions = async (): Promise<{ modifiedCount: number }> => {
  console.log('⏸️ [STUB] expireOverduePromotions called — no-op until Promotion module is built');
  return { modifiedCount: 0 };
};
