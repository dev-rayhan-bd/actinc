/**
 * Stub: Banner services — implement when Banner module is built.
 */
export const expireOverdueBanners = async (): Promise<{ modifiedCount: number }> => {
  console.log('⏸️ [STUB] expireOverdueBanners called — no-op until Banner module is built');
  return { modifiedCount: 0 };
};
